/**
 * Global site branding, metadata, and application configuration.
 */
export const SITE_CONFIG = {
  NAME: "AI Chat Client",
  DESCRIPTION:
    "AI chat client with support for streaming, tools and knowledge bases.",
} as const;
